[profile.default]
src = "ir-schema/examples/poc"
test = "ir-schema/examples/poc"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
fork_block_number = 22500000

[rpc_endpoints]
mainnet = "${ETH_RPC_URL}"
bsc = "${BSC_RPC_URL}"
